[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "python3-cyberfusion-fake-project"
version = "1.0"
description = "Fake project"
authors = [
    { name = "Cyberfusion", email = "support@cyberfusion.io" },
]
dependencies = []
